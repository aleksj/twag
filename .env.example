# ClickHouse Cloud service metadata.
CLICKHOUSE_SERVICE_ID=

# Optional: lets the CLI resolve CLICKHOUSE_HOST from ClickHouse Cloud.
# ClickHouse Cloud API uses basic auth: key id + key secret.
CLICKHOUSE_CLOUD_KEY_ID=
CLICKHOUSE_CLOUD_KEY_SECRET=
CLICKHOUSE_ORGANIZATION_ID=

# ClickHouse SQL connection settings.
# If CLICKHOUSE_HOST is empty, use `twag-clickhouse resolve-cloud-service`
# after setting the cloud API credentials above.
CLICKHOUSE_HOST=
CLICKHOUSE_PORT=8443
CLICKHOUSE_USERNAME=default
CLICKHOUSE_PASSWORD=

# Optional alias if your deployment calls the database password an API key.
# Used only when CLICKHOUSE_PASSWORD is empty.
CLICKHOUSE_API_KEY=

CLICKHOUSE_DATABASE=default
CLICKHOUSE_SECURE=true
CLICKHOUSE_CONNECT_TIMEOUT=10
CLICKHOUSE_SEND_RECEIVE_TIMEOUT=30
CLICKHOUSE_QUERY_RETRIES=3
CLICKHOUSE_RETRY_INITIAL_SECONDS=1
CLICKHOUSE_RETRY_MAX_SECONDS=8

# Subconscious model endpoint for the NYTechWeek agent wrapper.
SUBCONSCIOUS_API_KEY=
SUBCONSCIOUS_BASE_URL=https://api.subconscious.dev/v1
SUBCONSCIOUS_MODEL=subconscious/tim-qwen3.6-27b
SUBCONSCIOUS_RUN_ENGINE=tim-gpt
SUBCONSCIOUS_MAX_TOKENS=1200
SUBCONSCIOUS_ENABLE_THINKING=true

# Senso knowledge base mirror. The agent does not call Senso directly; the
# sync agent mirrors Senso into ClickHouse senso_* tables.
SENSO_API_KEY=
SENSO_BASE_URL=https://apiv2.senso.ai/api/v1
SENSO_ORG_ID=
SENSO_ORG_SLUG=
SENSO_SYNC_ENABLED=true
SENSO_SYNC_INTERVAL_SECONDS=3600
SENSO_SYNC_REPLACE=false
SENSO_SYNC_BATCH_SIZE=500
SENSO_CHUNK_CHARS=3500
SENSO_CHUNK_OVERLAP=300
SENSO_MAX_DOWNLOAD_BYTES=2000000

# Direct Tech Week calendar sync. This does not use Nimble or paid scraping
# APIs. It crawls the public NYC/Boston calendars, fetches changed Partiful
# detail pages gently, writes append-only calendar snapshots, exposes latest
# complete runs through <prefix>_current_events, and records row-level changes
# in <prefix>_sync_runs and <prefix>_sync_changes.
TECHWEEK_CALENDAR_SYNC_ENABLED=true
TECHWEEK_CALENDAR_SYNC_CITIES=nyc,boston
TECHWEEK_CALENDAR_SYNC_INTERVAL_SECONDS=21600
TECHWEEK_CALENDAR_SYNC_INCLUDE_INVITE_ONLY=false
TECHWEEK_EVENT_FETCH_CONCURRENCY=3
TECHWEEK_EVENT_FETCH_TIMEOUT_SECONDS=25
TECHWEEK_CALENDAR_SYNC_BATCH_SIZE=500
TECHWEEK_CALENDAR_MAX_SCROLL_TICKS=260
TECHWEEK_CALENDAR_SCROLL_DELAY_SECONDS=0.35
TECHWEEK_CALENDAR_PAGE_DELAY_SECONDS=0.1

# Active Tech Week city. Controls dataset path, ClickHouse table prefix,
# system prompt, and Telegram bot copy. Known values: nyc, boston.
TWAG_CITY=nyc

# Optional public sponsor copy shown in bot greetings.
TWAG_SPONSOR_LINE="**Sponsored by Data.Flowers** - https://data.flowers/ and **Subconscious** - https://www.subconscious.dev/"

# Geocoder for event venues. OpenCage free tier: 2,500/day, 1 req/sec,
# permanent storage allowed. Get a key at https://opencagedata.com/.
OPENCAGE_API_KEY=your-opencage-api-key-here

# Mapbox GL JS public token for the event map web page. Free tier
# covers ~50k loads/month. Restrict the token by referrer in the
# Mapbox dashboard (Account -> Tokens -> URL restrictions) before
# going live. The token is shipped to the browser in docs/config.js.
MAPBOX_PUBLIC_TOKEN=pk.your-mapbox-public-token-here

# Base URL where the static map pages are hosted (GitHub Pages).
# TWAG appends the active city's map file:
# - nyc -> events_map_nyc.html#date=YYYY-MM-DD
# - boston -> events_map_boston.html#date=YYYY-MM-DD
TWAG_PUBLIC_MAP_BASE_URL=https://natea.github.io/twag/

# Public ClickHouse query tool and remote sync-agent defaults.
NYTW_TOOL_URL=
NYTW_TOOL_TOKEN=
NYTW_TOOL_HOST=localhost
NYTW_TOOL_PORT=8000
NYTW_TOOL_RELOAD=false
NYTW_TOOL_ACCESS_LOG=false
NYTW_TOOL_SUPPRESS_SCANNER_NOISE=true
NYTW_TOOL_LOG_LEVEL=info
TWAG_SYNC_AGENT_COMMAND=.venv/bin/twag-sync-agent

# Browser terminal backend. Keep the default host local unless a reverse proxy
# on another host must reach this service.
TWAG_TERMINAL_HOST=localhost
TWAG_TERMINAL_PORT=8765
TWAG_TERMINAL_TOKEN=
TWAG_TERMINAL_RELOAD=false
TWAG_TERMINAL_ACCESS_LOG=false
TWAG_TERMINAL_LOG_LEVEL=info
TWAG_TERMINAL_SESSION_DIR=logs/terminal-sessions
TWAG_TERMINAL_AGENT_MAX_TURNS=4
TWAG_TERMINAL_ENABLE_THINKING=false
TWAG_TERMINAL_QUERY_TIMEOUT_SECONDS=180
TWAG_TERMINAL_QUERY_HEARTBEAT_SECONDS=20

# Telegram bot. Define one token per city; the active TWAG_CITY picks the
# matching <CITY>_TELEGRAM_BOT_TOKEN.
NYC_TELEGRAM_BOT_TOKEN=
BOSTON_TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_CHAT_IDS=
TELEGRAM_CLEAR_WEBHOOK_ON_POLL=true
TELEGRAM_POLL_TIMEOUT=30
TELEGRAM_REQUEST_TIMEOUT=45
TELEGRAM_RETRY_INITIAL_SECONDS=2
TELEGRAM_RETRY_MAX_SECONDS=60
TELEGRAM_STATUS_HEARTBEAT_SECONDS=8
TELEGRAM_STREAM_DRAFTS=true
TELEGRAM_STREAM_DRAFT_INTERVAL_SECONDS=1
TELEGRAM_WARM_CLICKHOUSE_ON_START=true
TELEGRAM_QUESTION_LOG_PATH=logs/twag-telegram-questions.jsonl

# Deployment target for deploy/ubuntu/rsync.example.sh. Keep real values in
# the ignored local .env file.
REMOTE_USER=ubuntu
REMOTE_HOST=
REMOTE_DIR=
SSH_PORT=22
RUN_REMOTE_INSTALL=false
DEPLOY_TERMINAL_STATIC=false
SYNC_ENV_FILE=true
REMOTE_ENV_FILE=/etc/twag/twag.env

# Optional static browser shell deployment. The backend deploy can call this
# when DEPLOY_TERMINAL_STATIC=true, or run deploy/terminal-static.sh directly.
TWAG_TERMINAL_STATIC_REMOTE=
TWAG_TERMINAL_STATIC_REMOTE_DIR=/var/www/html/tw
TWAG_TERMINAL_STATIC_SSH_PORT=22
TWAG_TERMINAL_STATIC_OWNER=root:root
